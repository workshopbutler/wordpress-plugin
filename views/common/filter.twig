{% macro filter(name, values) %}
    <select class="wb-filter" data-filter data-name="{{ name }}">
        {% for item in values %}
            <option class="wb-filter" value="{{ item.value }}">{{ item.name }}</option>
        {% endfor %}
    </select>
{% endmacro %}
