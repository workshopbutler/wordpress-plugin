{% set url = type == 'thumbnail' ? event.get_cover_image.get_thumbnail_url : event.cover_image.get_url %}
{% if url %}
    <img src="{{ url }}" alt="{{ event.get_title }}" width="{{ width }}" height="{{ height }}"
        {% if class %} class="{{ class }}" {% endif %}/>
{% endif %}
