<div class="wsb-info">
    <div class="wsb-info__title">{{ __('event.info.date', 'wsbintegration') }}:</div>
    {% if event.schedule.at_one_day() %}
        {{ wsb_f(event.schedule, 'full_long') }} {{ wsb_f(event.schedule, 'timezone_short') }}
    {% else %}
        {{ wsb_f(event.schedule, 'start_long') }}
        —<br>{{ wsb_f(event.schedule, 'end_long') }} {{ wsb_f(event.schedule, 'timezone_short') }}
    {% endif %}
</div>
